<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="ru.example.ivan.smssender">

    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.SEND_SMS" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />

    <application
            android:name=".App"
            android:allowBackup="true"
            android:icon="@mipmap/ic_launcher"
            android:label="@string/app_name"
            android:roundIcon="@mipmap/ic_launcher_round"
            android:supportsRtl="true"
            android:theme="@style/AppTheme">
        <activity android:name=".ui.screens.main.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".ui.screens.group.GroupActivity">
        </activity>
        <activity android:name=".ui.screens.messages.MessagesActivity">
        </activity>
        <activity android:name=".ui.screens.new_group.NewGroupActivity">
        </activity>
        <activity android:name=".ui.screens.contacts.ContactActivity">
        </activity>
        <activity android:name=".ui.screens.new_message.NewMessageActivity">
        </activity>
    </application>

</manifest>